{
  "name": "@omniproxy/gateway",
  "version": "0.0.0",
  "private": true,
  "description": "The HTTP gateway: standard dialects in front of provider web interfaces",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": { ".": { "types": "./dist/index.d.ts", "default": "./dist/index.js" } },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@omniproxy/dialect-openai": "workspace:*",
    "@omniproxy/engine-declarative": "workspace:*",
    "@omniproxy/schema": "workspace:*",
    "@omniproxy/transport": "workspace:*"
  },
  "devDependencies": {
    "@omniproxy/provider-sim": "workspace:*",
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
